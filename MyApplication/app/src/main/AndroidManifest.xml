<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cms.com.tn_ecs">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/e_tn_ecs_app_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.BaseTheme">
        <activity
            android:name=".activity.ContainerActivity"
            android:configChanges="orientation|keyboardHidden|fontScale"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateVisible|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PDFViewerActivity"
            android:label="@string/title_activity_pdfviewer"></activity>
        <activity
            android:name=".activity.CertificateViewer"
            android:label="@string/title_activity_certificate_viewer"></activity>
    </application>

</manifest>
